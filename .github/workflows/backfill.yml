name: backfill
# One-off / catch-up loads, run on demand from Actions (never the laptop):
#   board-meetings  results + board-meeting dates  (from/to = dates, default 2020-01-01..today)
#   prices          NSE bhavcopy -> daily_prices + bucket  (from = date; to defaults to the end of
#                   LAST month, so it never races the daily run's read-modify-write of this month)
#   orphans         companies rows for symbols seen in filings/deals/events but not the master
#   shareholding    quarterly SHP XBRL history, 30 quarters/symbol, 20k XBRL per run (from = floor, default 2020-01-01)
#   insider         PIT disclosures: the whole undated list (~5 months) when no from; else the from..to window
#   pref-issues     preferential allotments + lock-in expiries (from default 2023-01-01, the list's start)
#   credit-ratings  pre-2024 rating filings into `filings` (from/to default 2020-01-01..2023-12-31), then
#                   extract every pending rating filing -> credit_ratings (re-run if it reports a backlog)
#   ipos            re-read every chittorgarh IPO page (subscription fields; from/to = ids, default 1..3500)
#   ipo-gmp         investorgain grey-market premium series for every IPO listed on/after from (default 2017-01-01)
#   ipo-bids        NSE retail times-subscribed for mainboard IPOs missing it (from default 2020-01-01)
# One concurrency group per kind: backfills of different kinds run side by side and alongside the
# scheduled refresh (they write different rows); two of the same kind queue.
on:
  workflow_dispatch:
    inputs:
      what:
        type: choice
        options: [board-meetings, prices, orphans, shareholding, insider, pref-issues, credit-ratings, ipos, ipo-gmp, ipo-bids]
        required: true
      from:
        description: "start date YYYY-MM-DD (board-meetings / prices)"
        required: false
      to:
        description: "end date YYYY-MM-DD (default: today; prices: end of last month)"
        required: false
permissions:
  contents: read
concurrency:
  group: backfill-${{ inputs.what }}
  cancel-in-progress: false
jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
      WHAT: ${{ inputs.what }}
      FROM: ${{ inputs.from }}
      TO: ${{ inputs.to }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: pip install -r requirements.txt
      - name: run
        run: |
          set -euo pipefail
          case "$WHAT" in
            board-meetings) python -u scripts/refresh_events.py board-meetings --from "${FROM:-2020-01-01}" --to "${TO:-$(date +%F)}" ;;
            prices)         LAST_MONTH_END="$(date -d "$(date +%Y-%m-01) -1 day" +%F)"
                            python -u scripts/refresh_prices.py --from "${FROM:?from is required}" --to "${TO:-$LAST_MONTH_END}" ;;
            orphans)        python -u scripts/backfill_orphans.py ;;
            shareholding)   python -u scripts/refresh_shareholding.py --per-symbol 30 --xbrl-limit 20000 --floor "${FROM:-2020-01-01}" ;;
            insider)        if [ -n "$FROM" ]; then python -u scripts/refresh_insider.py --from "$FROM" --to "${TO:-$(date +%F)}";
                            else python -u scripts/refresh_insider.py --all; fi ;;
            pref-issues)    python -u scripts/refresh_prefissues.py --from "${FROM:-2023-01-01}" --to "${TO:-$(date +%F)}" ;;
            credit-ratings) python -u scripts/refresh_filings.py --ratings-only --from "${FROM:-2020-01-01}" --to "${TO:-2023-12-31}"
                            python -u scripts/extract_ratings.py --limit 15000 ;;
            ipos)           python -u scripts/refresh_events.py ipos --from-id "${FROM:-1}" --to-id "${TO:-3500}" ;;
            ipo-gmp)        python -u scripts/refresh_gmp.py --since "${FROM:-2017-01-01}" ;;
            ipo-bids)       python -u scripts/refresh_ipo_bids.py --since "${FROM:-2020-01-01}" ;;
          esac
