# Market-intel platform — Python deps (also installed by .github/workflows/*)
pandas>=3.0
numpy
requests          # NSE static CSVs, chittorgarh, screener.in, Supabase PostgREST
beautifulsoup4    # screener.in HTML parse
lxml              # bs4 parser backend
html5lib          # pandas read_html fallback parser (chittorgarh tables)
yfinance          # EOD prices (adjusted) + benchmarks
nselib            # NSE history: unadjusted prices, corporate actions, deals
jugaad-data       # EOD prices fallback (official NSE bhavcopy)
fastparquet       # parquet caches (price store, fundamentals)
openpyxl          # NSE serves some bhavcopies as .xlsx under the .csv name (2022-08-08)
pymupdf           # filing PDF text (F3 KPI extraction); same lib as Question_Bank ingestion
pytest            # tests
